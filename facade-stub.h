/*
 * facade-stub.h — empty runtime header for Amalgame.Hardware.Led.
 *
 * Pure-Amalgame driver (no @c). Exists only because [stdlib].header is
 * required by amc's PackageRegistry.LoadFrom; the #include is a no-op.
 */
